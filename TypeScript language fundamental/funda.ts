// backtic ``

let language : string = `angulerJS2`;
console.log(`I am learning ${language} language`);

function NameOfLangualge() : string {
    return language;
}

function thisIsVersion(version : number) {
    console.log(`Version: ${version}`)
}
NameOfLangualge();
thisIsVersion(4);
